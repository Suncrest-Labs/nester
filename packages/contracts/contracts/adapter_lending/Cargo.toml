[package]
name = "adapter-lending-contract"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
# Clean crate name so other crates can import it as `adapter_lending`.
name = "adapter_lending"
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }
nester-common = { path = "../../libs/common" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
nester-test-utils = { path = "../../libs/test_utils" }
