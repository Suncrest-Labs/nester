[package]
name = "nester-goal-rewards"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }
nester-common = { path = "../../libs/common" }
nester-access-control = { path = "../access_control" }

[dev_dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }