apiVersion: apps/v1
kind: Deployment
metadata:
  name: nester-api-staging
  namespace: nester-staging
  labels:
    app: nester-api
    env: staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nester-api
      env: staging
  template:
    metadata:
      labels:
        app: nester-api
        env: staging
    spec:
      containers:
        - name: api
          image: ghcr.io/suncrestlabs/nester-api:latest
          ports:
            - containerPort: 8080
          env:
            - name: APP_ENV
              value: "staging"
            - name: STELLAR_NETWORK
              value: "testnet"
            - name: STELLAR_HORIZON_URL
              value: "https://horizon-testnet.stellar.org"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: nester-staging-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: nester-staging-secrets
                  key: redis-url
            # Sourced from a ConfigMap rather than inlined: these are the
            # testnet addresses the staging deploy points at, and they change
            # whenever the contracts are redeployed. The literals that were
            # here were not valid Stellar contract IDs, so a cluster applying
            # this manifest unchanged came up pointing at nothing.
            - name: CONTRACT_ADDRESS_VAULT_FACTORY
              valueFrom:
                configMapKeyRef:
                  name: nester-staging-contracts
                  key: vault-factory-address
            - name: CONTRACT_ADDRESS_YIELD_REGISTRY
              valueFrom:
                configMapKeyRef:
                  name: nester-staging-contracts
                  key: yield-registry-address
            - name: FUNDED_TEST_ACCOUNT_SECRET
              valueFrom:
                secretKeyRef:
                  name: nester-staging-secrets
                  key: funded-test-account-secret
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: nester-api-staging
  namespace: nester-staging
spec:
  selector:
    app: nester-api
    env: staging
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
