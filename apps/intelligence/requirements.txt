fastapi==0.141.1
starlette==1.3.1
uvicorn==0.34.0
httpx==0.28.1
anthropic>=0.58.0
pydantic==2.10.4
pydantic-settings==2.7.1
PyJWT==2.13.0
slowapi==0.1.9
redis>=5.0.0
aiohttp>=3.9.0
cachetools>=5.3.0
pytest-asyncio>=0.21.0
stellar-sdk>=15.0.0
numpy==2.2.6
scipy==1.15.3

# Distributed tracing (nester#1054). Tracing is opt-in via
# INTELLIGENCE_TRACING_ENABLED; these are imported at startup but install
# nothing and dial no collector when it is off.
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
opentelemetry-exporter-otlp-proto-grpc==1.44.0

# Prometheus exposition for the intelligence SLIs (nester#1056). Client only;
# it starts no server of its own — the metrics route is mounted on the
# existing FastAPI app.
prometheus-client==0.21.1
