// The canonical security & audit page lives at /security. /audits is kept as an
// alias so existing links keep working and both routes render the same content.
export { default, metadata } from "../security/page";
